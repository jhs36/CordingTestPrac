def solution(my_string, is_prefix):
    answer = 0
    a = []
    
    for n1,n2 in enumerate(my_string):
        a.append(my_string[:n1+1])
    
    if is_prefix in a:
        answer += 1
    return answer
