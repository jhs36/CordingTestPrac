def solution(numbers):
    lista = []
    for x1, y1 in enumerate(numbers):
        for x2, y2 in enumerate(numbers):
            if x2 > x1:
                lista.append(y1*y2)
    return max(lista)
