def solution(n):
    listb = []
    for i in range(4,n+1):
        lista = []
        for x in range(1,i+1):
            if i%x == 0:
                lista.append(x)
        if len(lista) > 2:
            listb.append(i)
    return len(listb)
